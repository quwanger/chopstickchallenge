using UnityEngine;
using System.Collections;

public class CC_Timer : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {//this.GetComponent<GUIText>().text = GameObject.Find("CC_Level").GetComponent<CC_TimeManager>().GetCurrentTimeString();
	}
}
